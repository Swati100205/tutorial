#include<stdio.h>
include<conio.h>
void main()
{
int number;
printf("Enter an integer:");
// reads and stores input
scanf("%d", &number);
// displays output
printf('You entered: %d", number);
return 0;
}
